# --- Core dependencies (OpenRouter-only setup) -----------------------------
# fschat provides the conversation templates (fastchat.conversation); its base
# install does NOT pull in torch, so it is safe here.
fschat==0.2.32
numpy==1.26.3
pandas==2.1.4
protobuf==4.25.0
pytz==2023.3.post1
urllib3==2.1.0          # used to call the OpenRouter HTTP API
wandb==0.16.1
pyarrow
fastparquet
python-dotenv           # loads OPENROUTER_API_KEY from .env

# --- Disabled back-ends ----------------------------------------------------
# The packages below are only needed for models that are NOT served through
# OpenRouter (direct OpenAI API, Anthropic SDK, Google PaLM/Gemini SDK, and
# local GPU inference). Re-enable them if you turn those back-ends on.
# openai==0.28.0
# anthropic==0.10.0
# google-generativeai==0.3.1
# torch==2.1.0
# transformers==4.35.0
# accelerate==0.25.0
